const Landing = () => {
  return (
    <div>
      <button type="button" className="btn">
        Book now!
      </button>
      <button type="button" className="btn">
        Call Now
      </button>
    </div>
  );
};

export default Landing;
