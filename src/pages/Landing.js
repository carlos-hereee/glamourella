const Landing = () => {
  return (
    <section className="container">
      <button type="button" className="btn">
        Book now!
      </button>
      <button type="button" className="btn">
        Call Now
      </button>
    </section>
  );
};

export default Landing;
